
        <%= form_tag search_posts_path, method: :get do %>
            <%= label_tag :タイトル %>
            <%= text_field_tag :title, params[:title] %>
            <%= label_tag :講義名 %>
            <%= text_field_tag :lecture, params[:lecture] %>
            <!--
            <%= select_tag params[:status], options_for_select([["掲載中","掲載中"],["受け渡し中","受け渡し中"],["受け渡し完了","受け渡し完了"]], selected: 0) %>
            -->
            <br>
            <%= submit_tag "検索", class: "btn btn-block btn-outline-dark" %>
        <% end %>
        